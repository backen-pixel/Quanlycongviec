# APK debug copy at repo root (tải lên điện thoại; không nên commit)
TuBep-VoiceSync-debug.apk
crm-mobile-debug.apk
*.apk
!backend/uploads/app-releases/**/*.apk

# Cursor local config
.cursor/

# Local env files (contain secrets)
backend/.env
frontend/.env
.env
scripts/knowledge/.auth-session.json
scripts/knowledge/node_modules/

# Local data store cho external API keys (chứa secret) — KHÔNG commit
backend/data/api-keys.json

# APK reverse-engineering dumps (local only, chứa file name reserved trên Windows)
.apk-analysis/
