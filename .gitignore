# APK / mobile release binaries — KHÔNG commit (quá nặng)
TuBep-VoiceSync-debug.apk
crm-mobile-debug.apk
*.apk
*.aab
*.ipa
backend/uploads/app-releases/

# Cursor local config
.cursor/

# Local env files (contain secrets)
backend/.env
frontend/.env
.env
scripts/knowledge/.auth-session.json
scripts/knowledge/node_modules/

# Local data store cho external API keys (chứa secret) — KHÔNG commit
backend/data/api-keys.json

# Local UAT evidence (may contain internal identifiers; archive outside Git)
backend/.uat-evidence/

# APK reverse-engineering dumps (local only, chứa file name reserved trên Windows)
.apk-analysis/

# Database dump files
backend/uploads/_clone/*.dump
backend/uploads/_tmp_*
backend/uploads/_auth_*

# Local backup from CRM route split (regenerable from git history)
backend/src/routes/crm/core.js.bak

# Local scratch / JVM / clone junk — KHÔNG commit
_tmp_*
**/_tmp_*
hs_err_*.log
replay_*.log
.jscpd-report/
backend/supabase/.temp/
