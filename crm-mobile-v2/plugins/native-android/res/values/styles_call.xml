<?xml version="1.0" encoding="utf-8"?>
<resources>
  <style name="Theme.IncomingCall" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowBackground">#0A1628</item>
    <item name="android:statusBarColor">#0A1628</item>
    <item name="android:navigationBarColor">#0A1628</item>
  </style>
  <!-- MainActivity nạp RN ngầm khi trả lời từ màn khóa — không che UI cuộc gọi -->
  <style name="Theme.MainCallBoot" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:backgroundDimEnabled">false</item>
  </style>
  <!-- Chat từ bong bóng — sheet trong suốt, không animation "nhảy app" -->
  <style name="Theme.BubbleChatOverlay" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:backgroundDimEnabled">false</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
  </style>
  <!-- Soạn tin overlay — Activity adjustResize, bàn phím hoạt động -->
  <style name="Theme.BubbleCompose" parent="Theme.AppCompat.NoActionBar">
    <item name="android:windowIsTranslucent">true</item>
    <item name="android:windowBackground">@android:color/transparent</item>
    <item name="android:windowAnimationStyle">@null</item>
    <item name="android:backgroundDimEnabled">true</item>
    <item name="android:statusBarColor">@android:color/transparent</item>
    <item name="android:navigationBarColor">@android:color/transparent</item>
  </style>
</resources>
