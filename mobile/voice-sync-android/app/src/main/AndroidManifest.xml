<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Mạng: gọi API đồng bộ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Ghi âm từ micro (ghi chú thoại / thử nghiệm trong app) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Nhận biết trạng thái cuộc gọi (rung / offhook) — một số thiết bị dùng cho tích hợp ghi âm cuộc gọi -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- Số / hướng cuộc gọi từ nhật ký (app đồng bộ) -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <!-- Foreground: ghi micro khi có cuộc gọi -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Android 9 trở xuống: đọc file âm thanh trên bộ nhớ -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- Android 13+: đọc file nhạc/ghi âm do app khác tạo (file ghi cuộc gọi xuất ra Máy/Download…) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Android 13+: thông báo (nếu sau này có đồng bộ nền) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceSync"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".CallMonitorService"
            android:exported="false"
            android:foregroundServiceType="microphone" />
    </application>
</manifest>
